#include "main.h"

//#define MAX_SIZE 8

void main(void)
{
	//int n = MAX_SIZE, m = MAX_SIZE;
	//Grid grid;
	//char *str;

	//grid = create_grid(n, m);
	//if (!grid.is_success) return;

	//printf("Init finished\n");

	//str = str_grid(grid);
	//delete_grid(&grid);
	//if (!str) return;

	//printf("Convert grid in string finished\n");

	//printf("%s\n", str);
	//if (str)
	//{
	//	free(str);
	//	str = NULL;
	//}

	//int is_running = 1;
	//int key;

	//while (is_running)
	//{
	//	key = get_key();
	//	handler(key);
	//	if (key == 27) {
	//		is_running = 0;
	//	}
	//}

	run_game();
}
